import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(
  // eslint-disable-next-line indent
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);

